{% extends 'layout.html.twig' %}

{% block body %}
    {{ parent() }}
	<div class="main container">
		<div class="row well">
			<div class="col-md-6">
				<div class="page-header">
					<div  class="text-center">
						<h1>{{ comment.foro.title }}</h1>
					</div>
					
					<h2>{% trans %} New comment  {% endtrans %}</h2>
				</div>
				<div class="row">
					<div class="col-md-4 col-sm-4 col-xs-4  text-center   ">
						<h3>{% trans %} User name:  {% endtrans %}</h3>
					 
					</div>
					<div class="col-md-3 col-sm-3 col-xs-3  text-left vcenter  ">
					    	<p> {{comment.user.nick}}</p>
					</div>
				</div>
				
				{% include 'userBundle:Comment:form.html.twig' with { 'form': form } %}
				
			</div>
		</div>
	</div>
{% endblock %}
